These are the files that go along with this lecture

The node_modules folder does not exist. To create it, run
npm install


To start webpack
webpack -w

Install live-server
npm install -g live-server

Run live-server
live-server OR live-server --port=8000